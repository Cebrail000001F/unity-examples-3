using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjeEtkilesimYontemleriIki : MonoBehaviour
{
    MeshCollider meshColliderNesne;
    void Start()
    {

        /*
        meshColliderNesne = GetComponent<MeshCollider>();
        meshColliderNesne.convex = true;
        meshColliderNesne.isTrigger = true;
        */
        /*
        MeshCollider[] meshColliderNesne = GetComponents<MeshCollider>();
        for (int i = 0; i < meshColliderNesne.Length; i++)
        {
            meshColliderNesne[i].isTrigger = true;
        }
        */

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
